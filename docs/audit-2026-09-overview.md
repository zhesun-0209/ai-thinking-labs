# AI 思维网站最终集成审计

日期：2026-09-16。范围：当前仓库已实现的第 5–12 章、章节目录、8 个实验列表、24 个 Notebook 阅读与下载页、旧入口，以及静态发布流程。第 1–4 章不在本仓库，不能声称已检查。课程与 Notebook 保留各自适合的案例，不以案例一致为验收条件。

## 结论

综合评分 **82/100，Conditional（有条件发布）**。已修复本轮发现的阻断性教学、交互和目录输入问题，可以更新现有 GitHub Pages。该评分是基于检查证据的人工评估，不是 Lighthouse 分数。新域名的服务器、外网 HTTPS、真实网络速度尚未验收，不能把本地迁移包视为已经完成腾讯云上线。

| 维度 | 得分 / 5 | 权重 | 主要证据 | 剩余边界 |
| --- | ---: | ---: | --- | --- |
| 功能完整性 | 4.5 | 20 | 课程跳转、40 个实验标签、步进、搜索、下载链接、复制与原图查看 | 未在全部浏览器逐一测试剪贴板权限状态 |
| 用户体验 | 4 | 15 | 同页实验独立状态、继续阅读、清晰空结果、知识点独立成行 | 没有真实初学者可用性访谈 |
| 视觉设计 | 4 | 15 | 统一浅色、中文无衬线字体、紧凑标题与控件，减少卡片套卡片 | 历史科学图的配色保留，不伪称全部重新制图 |
| 性能 | 3.5 | 10 | 阅读页共享 CSS，移除重复 JupyterLab 应用样式，静态白名单发布 | 两个扩散页仍大于 1.2 MB，未测 LCP/INP/CLS |
| 响应式 | 4.5 | 10 | 八章三档布局无页面横溢；24 本移动/桌面检查 | 宽表格和代码保留局部滚动；不是全设备覆盖 |
| 工程质量 | 4 | 10 | 17 个 JS 语法检查；发布流程 12 项测试；确定性输出打包 | 历史 CSS 层叠仍多，没有扩大为全站框架重构 |
| 安全 | 4 | 10 | 修复目录参数拼接、白名单阅读入口；发布不含私有目录 | 没有登录系统；未进行云主机渗透测试 |
| SEO / 无障碍 | 4 | 10 | 单一 h1、焦点、键盘滑块、图像描述、目标域名元数据与站点地图 | 未做屏幕阅读器完整验收；课程主体仍依赖 JS |

## 优先问题与修复

| 级别 / 视角 | 原问题及影响 | 修复与落点 |
| --- | --- | --- |
| P1 / 教师 | MiniMax 选错分支并错误剪枝；DFS 路径来源与累计代价不一致 | `app.js` 保留原数据，按实际运算修正；最终 MiniMax 选 +8，DFS 代价 13 |
| P1 / 教师 | 熵、梯度下降、感知机、注意力、困惑度、TD 等展示值无法由给定数据复算 | 修正第 7–11 章的真实计算或补全计算条件；详见逐章报告 |
| P1 / 教师 | 第 6 章文学图谱含错误发表/获奖关系；SFT 被解释成回答格式 | 核对事实，修正关系；明确提示不改参数，SFT 用示范数据训练参数 |
| P1 / 教师 | MDP 总览反馈方向错误，Skip-gram 图将 Softmax 与负采样串联 | `js/course-architectures.js` 改成智能体→动作→环境→新状态/奖励的闭环，以及正/负样本训练分支 |
| P1 / 用户 | TD 初始图已显示后续答案；校园图提前高亮最终路线且道路被画成单向 | 按步骤揭示 TD 数值；校园双向道路不画箭头，只高亮连续路径边，结束时才显示最终路线 |
| P1 / 教师 | 冰湖 MCTS 把根回报回传给所有节点，循环状态混用价值，以 UCT 作为最终动作排名 | 只重做这一册：有限深度节点、逐节点后续回报、访问次数选动作、真实抽样后逐步重新规划；其余 23 本下载文件不变 |
| P1 / 用户 | 同一算法在正文与实验室共享 DOM id 和定时器，切换后容易串状态或重复计时 | `js/course-shared.js` 为每个实例生成独立标识，切换清理计时器，手动步进停止播放 |
| P1 / 安全 | 实验列表把无效章节参数拼入 HTML，旧阅读入口接受任意 Notebook 字符串 | 固定章节范围、转义文本、24 本文件白名单，无效入口提供返回路径 |
| P1 / 发布 | 原部署可把源码、报告等整个工作目录发布，并重算耗时的模型 Notebook | 发布仅使用 85 项源文件白名单及 3 项生成文件；CI 不执行 Notebook 或下载模型 |
| P2 / 产品 | 工程说明、重复章节标题、核心/拓展及时间标签挤占阅读空间 | 重写目录与阅读外壳；列表保留标题、简介、独立知识点、阅读/下载；不增加“这页如何使用”的段落 |
| P2 / 前端 | 首页卡片过重，课程、深色实验室、Notebook 风格割裂 | 目录改为阅读列表；统一白底、深色正文与克制青绿主色；章节、实验室采用平铺布局 |
| P2 / 前端 | 移动端导航和长代码挤出页面，固定头部覆盖锚点 | 可横向滚动导航、动态头部高度、局部代码/表格滚动、清晰焦点与触控尺寸 |
| P2 / 用户 | 输出图缩小后难辨细节，图像描述泛化 | 保留原始 PNG，补每图描述与尺寸，提供原分辨率对话框、Escape 关闭和焦点返回 |
| P2 / 性能 | 每个导出 HTML 都带约 26 万字符的重复应用样式 | 导出器使用共享 `notebooks.css`，保留 cell / Markdown / 输出结构，不压缩原图冒充内容保全 |
| P2 / 前端 | 浅色化后第五章步骤数对比不足、播放按钮换行、MiniMax 计数不随滑块更新 | 修正文字颜色、固定图标控件尺寸、同步 MiniMax 步数 |
| P2 / 迁移 | 硬编码 GitHub 子路径会导致根域部署链接、canonical 和 404 不正确 | 构建器按目标 URL 改写元数据与已知路径；分别验证项目子路径及根域 |

## 逐章教学检查

完整证据、计算与来源见 [课程内容报告](audit-2026-09-content.md)。

| 章节 | 重点检查及结果 |
| --- | --- |
| 5 搜索 | 五种路径搜索累计代价 13/8/7/8/7；MiniMax 完整分支值 -100/-10/8；图例、双向边、逐步路径与结束状态复核 |
| 6 推理 | 事实/规则方向、前向与后向链、关系约束、多跳查询和证据排序；文学事实已纠正，权重不冒充客观结论 |
| 7 学习 | 信息增益分支样本完整；MSE 用实际梯度逐次下降；感知机记录实际错分点与更新；聚类和指标含义复核 |
| 8 连接 | 补反向传播的输入、权重、偏置和损失条件；注意力实际 Softmax；TransE 零损失不再假装继续学习 |
| 9 语言 | BPE 合并字与词表解释、负采样、注意力、下一词概率与困惑度、SFT/CoT 区别；第 4 个续写概率补全 |
| 10 感知 | 卷积、ViT、MAE、CLIP 过程与图示；InfoNCE 分母包含锚点的正负候选；Notebook 的既有真实模型输出保留 |
| 11 行动 | 奖励下标和累计回报 0/1/2.8/10.9，终态剩余价值为 0；TD 2→2.52 按阶段揭示；优势是估计而非精确优势 |
| 12 创造 | 退火确实暂时接受差解；课程 MCTS 区分单次回报/累计统计；GAN 的 0.5 不当作质量证明；AlphaFold 限定版本与能力 |

Notebook 外壳与导出保全证据见 [Notebook 报告](audit-2026-09-notebooks.md)。其中初次 24 本只导出后的全保全结论，是 MCTS 数值修复之前的基线；最终版本仅 MCTS 例外重新执行，不能继续宣称 24 本字节均不变。

## 浏览器与命令验收

- 浏览器为 Codex 内置浏览器。测试站点为 `http://127.0.0.1:8772/`，共享课程资源版本为 `20260916-r3`，第五、十二章最终脚本为 `20260916-r4`。
- 设备尺寸请求为 390、768、1440px；保留用户浏览器缩放，记录到的 CSS 宽度为 **339、668、1252px**。中途发现连续修改视口未及时生效，丢弃该批不稳定记录，重置视口后按每档重新验证八章。
- 三档八章检查均为单一 h1、无空演示容器、无全页横向溢出。不是以 `overflow-x:hidden` 的存在替代人工验收；宽表格和代码采用自身滚动区域。
- 第 6–12 章 **34 个实验标签**逐一切换并用键盘到达末步，状态及滑块均匹配，图示容器有内容。另检查第 5 章 5 种图搜索及 MiniMax，路径最终边数为 4/2/3/2/3。
- 24 个 Notebook 阅读页完成移动端及桌面外壳扫描；复制按钮与可见代码对应，图像有尺寸与具体描述，单一 h1，无全页横溢。MCTS 修改后另外检查新图与过程文字。
- 已实际测试：目录搜索命中、空结果与 Escape 清空；继续阅读；章节导航；正文/实验室独立步进；键盘滑块及标签；复制代码；原图打开、Escape 关闭和焦点恢复。
- 17 个公开 JavaScript 文件 `node --check` 通过，`git diff --check` 通过。
- 发布工具 **12/12 测试通过**，包括真实本地 Caddy HTTP 服务、缓存、gzip、404、旧路径跳转、打包文件一致性、私有文件排除、模拟激活失败恢复及回滚。最新复验耗时 10.321 秒。
- 课程内容的独立数值断言及 MCTS 单本执行证据见课程报告。没有为了网页样式更新重新下载或执行其他 23 本模型。

最终 24 个阅读 HTML 合计 8,793,008 字节，相比原 15,070,293 字节减少约 **41.65%**。这包含 MCTS 新增过程与输出，区别于 Notebook 外壳子任务的 42.14% 基线。23 本下载文件逐字节未变；全部 24 本存储输出无 error，也未检出安装下载日志或缺字/字体警告。

最终本地产物在仓库外：`/Users/zhesun/Desktop/Fudan/phd/shapeofai-release-20260916`（新域名）和 `/Users/zhesun/Desktop/Fudan/phd/shapeofai-github-release-20260916`（原项目路径）。两包各有 88 个公开文件；构建不修改 Notebook。新域名归档约 11.64 MB，不包含审计报告、部署凭据、开发脚本或 Git 历史。完整哈希见 [部署验收](../deploy/validation.md)。

## 迁移安排与未验证项

目标域名为用户提供的 **shapeofai.cn**。已选择新加坡轻量应用服务器通用型，2 核 2 GB、60 GB SSD、30 Mbps、1024 GB/月、35 元/月，先月付；只确认方案，未购买。

公开 DNS 查询显示 DNSPod 权威 NS，不是 Cloudflare。没有因用户给过 Cloudflare 控制台链接而贸然切换解析。服务器选型及官网依据见 [选型报告](tencent-server-options.md)，安装、部署、域名和回滚流程见 [迁移文档](migration-tencent.md)。

仍未验证：实际腾讯云账号库存/成交价、服务器 SSH、Ubuntu 服务运行、云防火墙、外网 TLS 与续期、DNS 切换、各运营商连通性、访问量压测、Safari/Firefox、完整屏幕阅读器使用和真实初学者理解度。静态站点不承担在线 Python 执行或模型训练。

不在本轮扩大处理的限制：部分课程图是明确的教学示意；A* 固定图起点启发式不满足一致性，但已复算本例最优解并限定一般保证条件；Notebook 仍保留既有模型依赖和下载条件。没有把以上情况写成“所有网络、模型和安装环境均保证可用”。
